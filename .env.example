# Backend/server env vars (do NOT commit .env with real secrets)
# Copy this file to `.env` in the project root and fill in values.

# Local backend server port (optional)
PORT=5177

# Gmail SMTP (use a Gmail App Password, not your normal password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=yourname@gmail.com
SMTP_PASS=your_16_char_app_password
SMTP_FROM=MindMate Reports <yourname@gmail.com>

# Optional: other backend secrets (left here as placeholders)
# AUTH_JWT_SECRET=change_me
# NEON_DATABASE_URL=postgresql://...
# No env required for local demo auth.
# You may set VITE_PORT if you want to change the dev server port.
# VITE_PORT=5173
